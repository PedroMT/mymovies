<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@android:color/darker_gray"
    tools:context="br.com.pmtsouza.mymovies.activities.MainActivity">

    <ImageView
        android:id="@+id/logo"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="32dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="logo"
        android:src="@drawable/dummy_logo"/>

    <Button
        android:id="@+id/myList_button"
        android:layout_margin="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Minha lista"
        android:textAllCaps="true"
        android:textColor="@android:color/black"
        android:background="@drawable/button_bg"
        android:layout_below="@+id/logo"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/searchMovies_button"
        android:layout_margin="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pesquisar filmes"
        android:textAllCaps="true"
        android:textColor="@android:color/black"
        android:background="@drawable/button_bg"
        android:layout_below="@+id/myList_button"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
